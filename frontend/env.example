# Frontend Environment Configuration
# The frontend doesn't require API keys as it communicates through the backend

# Backend API URL (default for local development)
VITE_API_URL=http://localhost:8080

# Note: All API keys are handled by the backend for security
